---
name: hippo-enh-t16-jit
description: "Enhancement Tier T16 (round 3, \"Point-of-action recall\") \u2014 shipped 2/2 items (JIT-1..2); PR #61 MERGED same day (squash 0489fe3, owner-ratified, on top of v1.18.0+SLP-2-fix, no version bump): JIT-1 first-touch reminder (RATIFIED default-ON, HIPPO_DISABLE_JIT kill switch; touchmap.json written at SessionStart, observe_touch on the PostToolUse spawn, measured p95 0.25ms vs the stated 50ms budget) + JIT-2 touch-grain outcome evidence (sparse cited_by rows, injection_hits grain=\"touch\", --touch-grain report-only; session grain stays default); next tier T17 EXT \u2014 EXT-1 recall on the PR diff is EXPLORATIONS3 \u00a75's highest new-user-value move"
metadata:
  type: project
  last_verified: "2026-07-16T14:49:37.820996+00:00"
  cited_paths: [".github/workflows/release.yml", "plugin/memory/jit.py", "tests/test_jit.py"]
  source_commit: "88ed8ebcb1e739a756b272b58438192e4c0ef70f"
  source_commit_time: 1784216675
---

T16 JIT implemented 2026-07-16 in one session per the capstone protocol; **Fred ratified and squash-MERGED PR #61 the same day (0489fe3 on main, branch deleted both sides; all CI lanes green)** (2 id-prefixed commits, tests-first, suite 2246 green at the tip plus the slow+scale lanes). Mid-session a concurrent session released v1.18.0 (5779b60) and the SLP-2 XML-escape fix (bf57e6c), and the enh-t16-jit branch pointer got repositioned in the process — the work was cherry-picked cleanly onto the new main tip and the full suite re-run there; local main was restored to origin exactly. Not a release PR (no version bump) — T16 then RELEASED same day in **v1.19.0 "At the act, and beyond the session"** (release PR #64, squash 88ed8eb, tagged, release.yml green), alongside T17 EXT, the SLP-2 plist fix and COR-20/RCH-10: the release that COMPLETED the round-3 train (T14–T17 all on main, all four ratified owner decisions shipped as ratified).

**What shipped:**
- JIT-1 (`2610695`) `plugin/memory/jit.py` + `tests/test_jit.py`: SessionStart's `_build_run_context` (the same offline, trust-gated moment that writes stale.json) persists `<index_dir>/touchmap.json` — cited path → the steer:pin/feedback memories citing it (floor-linked names excluded: the MEMORY.md floor is already always-loaded), descriptions pre-bounded. `observe_touch` rides the PostToolUse hook's existing SINGLE Python spawn (`memory.outcome --from-hook`): derived-cache reads only, empty-norm fast path; first touch of a mapped file per session emits ONE line — `memory <name>: <description>` — as hook additionalContext. Bounds: ≤200 chars/line, ≤3 lines/session, once per (file, session), memory-grain dedup, recall_events suppression (never duplicate an injection the model just saw), SEC-1 trust re-checked lazily on the emit path, `HIPPO_DISABLE_JIT` restores pre-T16 byte-for-byte (STABILITY.md documents it). Measured (scale lane, the shipping gate): p50 0.04ms / p95 0.25ms / max 8.8ms per touch decision vs the stated 50ms budget at 500 memories; cache build 100ms vs 5s; touchmap 9.3KB; a default-lane negative test pins ZERO corpus opens on the hook path. Both new writers use write_json_atomic and are CRASH_CONTRACT-registered (write_touch_cache detected; _write_state intact).
- JIT-2 (`eeef333`) `telemetry.log_outcome` gains OPTIONAL sparse `cited_by` (rows without it keep the exact pre-T16 key set — additive schema, session-grain consumers untouched); `record_from_payload` threads the lane's touch-time lookup onto the row (caps: 40 rows/session, 16 names/row); `outcome.injection_hits(grain="touch")` joins on the RECORDED coincidence via the ONE shared `_injection_join` (semantics cannot fork); `python -m memory.outcome --touch-grain` prints the report-only comparison. Dogfooded live on this repo: session grain 28 memories/116 hit-sessions vs touch grain 0/0 (the honest pre-recording baseline; the under-count direction is the spec's named risk — touch grain is evidence-plus, never evidence-instead).

**Why:** every recall moment was prompt-shaped; the moment a feedback lesson matters most is the ACT — the first edit to the cited file, possibly an hour after injection fell out of the context window. The ratified default-on decision leans on the empty-norm design: most touches emit nothing, ever.

**How to apply:** the four ratified contract changes now live on main (the PostToolUse hook may emit one bounded hookSpecificOutput JSON; HIPPO_DISABLE_JIT in STABILITY.md; the touchmap.json + jit/ state derived files; the sparse cited_by ledger field). Dogfood: touch a file cited by a feedback memory and expect the one-line reminder; `--touch-grain` populates as post-merge rows record. The eeef333/2610695 shas above are branch commits squashed away by the merge — 0489fe3 is the anchor on main. Next baton: T17 EXT, the LAST round-3 tier — EXT-1 (recall on the PR diff, a sticky CI comment; quiet dogfood on this repo first per the ratified positioning) ranks highest; EXT-2 cross-project promotion mining and EXT-3 the consolidate interview step follow. Related: [[hippo-enh-t15-sleep]], [[hippo-enh-t14-invariants]], [[hippo-enhancement-roadmap]].

Related: [[hippo-enh-t15-sleep]], [[hippo-enh-t14-invariants]], [[hippo-enhancement-roadmap]]
