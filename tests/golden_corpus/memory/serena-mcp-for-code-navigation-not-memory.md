---
name: serena-mcp-for-code-navigation-not-memory
description: "use the code-navigation MCP for symbol lookup, not as a place to persist long-term memory"
metadata:
  type: feedback
---

A tool built for finding/renaming symbols across a codebase is the wrong place to
stash durable knowledge — that belongs in the memory corpus, not a code-nav server's state.
