---
name: rtl-layout-mirrors-not-just-text-direction
description: "right-to-left languages mirror the whole layout, not just the text run direction"
metadata:
  type: reference
---

Icons, progress bars, and navigation chevrons need to flip horizontally in RTL
locales (Arabic, Hebrew) — mirroring only the text direction looks broken.
